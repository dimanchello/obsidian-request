export const DEFAULT_AUTO_HEADERS = [
    { key: 'Accept', value: '*/*', enabled: true, auto: true },
    { key: 'Connection', value: 'keep-alive', enabled: true, auto: true },
    { key: 'Accept-Encoding', value: 'gzip, deflate, br', enabled: true, auto: true }
]

export const SIDEBAR_MIN_WIDTH = 150
export const SIDEBAR_MAX_WIDTH = 500

export const RESPONSE_PANEL_MIN_HEIGHT_PCT = 10
export const RESPONSE_PANEL_MAX_HEIGHT_PCT = 85

export const DROPDOWN_BLUR_DELAY_MS = 200

export const CONTEXT_MENU_WIDTH = 200
export const CONTEXT_MENU_HEIGHT = 120

export const FALLBACK_CLIENT_HEIGHT = 1000
